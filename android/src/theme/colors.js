// Same hex values as tailwind.config.js's color tokens (which mirror
// server/src/index.css's @theme block) — kept here too because lucide-react-
// native icons take a `color` prop directly rather than picking up a
// className's text-color the way a NativeWind-styled <Text> does.

export const colors = {
  ink900: "#15181c",
  steel700: "#3a4046",
  steel300: "#c7cdd3",
  chalk50: "#f1f2f0",
  surfaceWhite: "#ffffff",
  signalYellow: "#f2b705",
  rubberRed: "#c43d3d",
  turfGreen: "#3f8f5f",
};
