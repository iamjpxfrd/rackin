// Kinetic Court palette (Decisions/UI Port Uses NativeWind's direction,
// chosen 2026-08-22 — see the Rackin Obsidian vault's Decisions index).
// Same hex values as tailwind.config.js's color tokens — kept here too
// because lucide-react-native icons take a `color` prop directly rather
// than picking up a className's text-color the way a NativeWind-styled
// <Text> does.

export const colors = {
  page: "#0a0a0a",
  card: "#141414",
  border: "#262626",
  hairline: "#1e1e1e",
  textPrimary: "#ffffff",
  textMuted: "#8a8a8a",
  textDim: "#666666",
  accent: "#d4ff3f",
  danger: "#ff5c5c",
};
