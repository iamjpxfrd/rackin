# Tests must not require a running PostgreSQL. This file shadows the main
# application.properties, so pinning the profile here keeps application-dev
# (and its Postgres datasource) out of the test context entirely.
spring.profiles.active=test
